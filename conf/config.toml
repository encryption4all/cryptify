[global]
server_url = "https://postguard.nl/"
address = "0.0.0.0"
data_dir = "/tmp/data"
email_from = "noreply@postguard.nl"
smtp_url = "mailcrab"
smtp_port = 1025
# smtp_username = "user"
# smtp_password = "pw"
allowed_origins = [
    '^https://(.*\.)?postguard\.eu$',
    '^https://(.*\.)?postguard\.nl$',
    '^https://(.*\.)?postguard\.email$',
    '^https://(.*\.)?postguard\.org$',
    '^https://(.*\.)?postguard\.app$',
]
pkg_url = "https://postguard-main.cs.ru.nl/pkg" 
